module.exports = [`

  # Allowed queries:
  type Query {
    # A simple type for getting started!
    hello: String
  }

  type Mutation {
    _empty: String
  }

  schema {
    query: Query
    mutation: Mutation
  }

`];
